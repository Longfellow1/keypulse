📍 Asia/Shanghai

# 2026-05-06

## 今日要点

今天最实在的进展在 KeyPulse HUD——你把信号源从零散事件改成读取日报「今日主线」主题，还把「今日的事儿」输回 inline 模式，凌晨提交了代码。但这个改动来回折腾了多轮，跨 4 个应用反复切换，核心是没把日报主线信号源的解析逻辑理顺。CorpusFlow 那边也明确了方向：不做完整 SFT 工厂，先聚焦批量 Query 生成和数据增强。

## 今天做的事

### KeyPulse HUD 信号源与输入模式优化

你花了大半天在这个改动上，在多个应用间反复切换——核心是让 HUD 的「今天最新」信号改吃 daily.md 里「## 今日主线」下的 H3 主题，点击还能跳转到对应段落；同时把「今日重要的事儿」从弹窗改回直接 inline 输入。过程中反复回到现场 5 次，最后在凌晨 00:05 提交了代码，修改 4 个文件， commit 信息是“fix(hud): signals 改吃日报「今日主线」主题 + 今日的事儿改回 inline 输入”。

### KeyPulse HUD 启动与功能验证

上午 09:24 你在终端执行 `keypulse hud &` 启动 HUD，之后又多次回来验证功能。这部分和信号源改动是套在一起的，每次改完代码都要启动看效果，所以成了反复回访的重灾区。

### KeyPulse HUD 代码提交与评审准备

下午 14:21 你整理了提交记录，提到这次修改有 176 处新增、98 处删除，短哈希 21c290d。之后还在 Claude 里处理项目文档评审建议，不过评审问题没在今天解决。

### KeyPulse HUD 后台运行逻辑确认

下午 15:44 你确认了一个细节：HUD 退出后状态栏图标会消失，但后台采集 daemon 会继续运行。这个点记下来是为了后续用户操作路径优化。

### CorpusFlow 产品定位收缩决策

你在 WPS 和 Codex 里明确了 CorpusFlow 的边界——当前应定义为“可控的批量评测 query 与数据增强工具”，不是完整 SFT 工厂或评测平台。核心用户是测试团队、数据 PM 和算法工程师，短期先打穿批量上传 seed、高质量 query 生成、参数泛化这些需求，多轮上下文和精调功能都往后放。

### CorpusFlow 项目仓库与功能定义

下午 15:32 你看了 GitHub 上的 CorpusFlow 仓库，描述是“一个为指令微调数生成而服务的、用户交互和界面友好的平台”。这和上午的定位收缩决策是一致的，相当于把方向落进了项目描述里。

### CorpusFlow 指令微调数据格式规范

下午 16:03 你整理了数据格式要求：统一 System Prompt 可选，必须在上传文件中提供 instruction/query/question/user 等字段，instruction 数据列读取为上传文件内容。这是为了解决之前“微调+多轮逻辑错了”的问题，把 LlamaFactory 的字段关系理清楚。

### 杂项

今天穿插了些零散操作：看 WPS 时遇到乱码；查奇瑞股价波动原因；研究 Claude Max 价格；执行 `python3 cleanup.py` 清理脚本；访问 Apple 报告问题页面；决定 RAG 项目转线下并模块化复用；用 tmux 启动 codex；浏览望京租房信息和 BOSS 直聘简历；确认 Gradio 前端还在运行。

## 今天的卡壳

KeyPulse HUD 的信号源切换改得很费劲——跨 4 个应用反复切换 5 次，中间还在终端和编辑器之间来回试错，主要是没把日报主题的解析逻辑（比如怎么准确提取「## 今日主线」下的 H3）一次捋顺，导致改了又改。

## 明日的锚点

> 明天我想：______
>
> _写一句话留给明天的自己_