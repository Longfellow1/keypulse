{
  "type": "object",
  "required": ["scope_week", "daily_topic_status_snapshots", "topics", "event_counts", "cross_week_diff", "key_decisions", "visible_outputs"],
  "properties": {
    "scope_week": {"type": "string", "pattern": "^\\d{4}-W\\d{2}$"},
    "daily_topic_status_snapshots": {"type": "array"},
    "topics": {"type": "array"},
    "event_counts": {"type": "object"},
    "cross_week_diff": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["topic", "from_state", "to_state"],
        "properties": {
          "topic": {"type": "string"},
          "from_state": {"type": "string"},
          "to_state": {"type": "string"}
        },
        "additionalProperties": false
      }
    },
    "key_decisions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["date", "text", "source"],
        "properties": {
          "date": {"type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"},
          "text": {"type": "string"},
          "source": {"type": "string"}
        },
        "additionalProperties": false
      }
    },
    "visible_outputs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["date", "text", "source"],
        "properties": {
          "date": {"type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"},
          "text": {"type": "string"},
          "source": {"type": "string"}
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
