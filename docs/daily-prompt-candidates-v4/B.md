📍 Asia/Shanghai

# 2026-05-06

## 今日要点

今天最花精力的是 KeyPulse HUD 的功能重构，你在 4 个应用间反复切换了 5 次，最终把信号源切换到日报「今日主线」主题，并改回 inline 输入模式。CorpusFlow 则明确了新定位——只做可控的批量评测 query 工具，放弃完整 SFT 工厂的想法，这是个重要的范围收缩决策。

## 今天做的事

### [KeyPulse] HUD 信号源与输入模式重构
从 00:05 到 21:07，你在 Terminal、Claude、代码编辑器和浏览器之间反复切换，最终完成两个核心改动：一是让「今天最新」信号改吃 daily.md 里「## 今日主线」下的 H3 主题，点击能直接跳转到对应段落；二是把「今日重要的事儿」改回 inline 输入，支持直接打字回车保存。这个过程中你反复回到开发现场，光是 HUD 相关的代码就改了 4 个文件，176 处新增、98 处删除。

### [KeyPulse] HUD 启动测试与行为确认
09:24 你执行 `keypulse hud &` 启动 HUD 进行测试，下午发现退出时状态栏图标会消失，但后台采集 daemon 会继续运行，这个行为符合预期。

### [KeyPulse] HUD 功能优化代码提交
14:21 你提交了凌晨写好的代码，commit 信息是「fix(hud): signals 改吃日报「今日主线」主题 + 今日的事儿改回 inline 输入」，短哈希 21c290d。

### [CorpusFlow] 产品范围收缩决策
你明确 CorpusFlow 当前应定义为「可控的批量评测 query 与数据增强工具」，不是完整 SFT 工厂或评测平台。核心用户锁定测试团队、数据 PM 和算法工程师，短期只做批量上传 100-200 seed、高质量 query 生成、参数泛化这些强需求，多轮上下文先做开关，精调/LLaMA-Factory 只导出契约，代码生成、Bad Case 诊断都往后放。

### [CorpusFlow] 微调与多轮逻辑问题排查
你发现 CorpusFlow 的微调+多轮逻辑有问题：「按照 llamafactory, system 是可选，但 instruction 是必填（就是提示词）、input 是当前 query、output 是最终输出」，之前把这几个关系搞混了，导致交互流程串不起来，填写和确认按钮都不生效。

### [CorpusFlow] RAG 方案调整
你决定不再用 ragflow，全面转线下，但要求这个 RAG 项目做完后能模块化复用。

### [GitNexus] 健康检查与 CI 稳定性修复
今天合并了 4 个修复：加宽 worker pool 重试超时防止 CI 抖动、给容器健康检查加专用 endpoint、API 嵌入后刷新 WAL 让搜索能看到新向量、用 ipKeyGenerator 处理 IPv6 子网归一化。

### [杂项]
你还看了奇瑞股价波动分析、Claude Max 价格页面、Apple 问题报告页面、望京转租信息和 BOSS 直聘简历模板。

## 今天的卡壳

下午排查 CorpusFlow 问题时，你发现微调与多轮逻辑根本理不清：「精调、批量（可能）里面的微调+多轮逻辑就错了...你把关系理清楚行不行？不然怎么做的明白？」当时有点 frustration，不得不停下来重新对照 LLaMA-Factory 的文档捋字段关系。

## 明日的锚点

> 明天我想：______
>
> _写一句话留给明天的自己_