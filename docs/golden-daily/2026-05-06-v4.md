📍 Asia/Shanghai

# 2026-05-06

## 今日要点

今天最耗精力的是 KeyPulse HUD 的功能优化——在多个工具之间来回切，反复回到现场才把逻辑理顺。核心改动是让信号源改吃日报「今日主线」主题，同时把「今日重要的事儿」改回 inline 输入。CorpusFlow 则明确了产品定位收缩：短期只做批量 Query 生成，精调、多轮上下文等功能都往后放。

## 今天做的事

### KeyPulse HUD 信号源逻辑修改

凌晨 00:05 你就开始处理这个改动，目标是让 HUD 信号源解析 daily.md 里「## 今日主线」下的 `### XXX` H3 标题，还得支持点击跳转到对应段落。同时把「今日重要的事儿」改回直接打字、回车保存的 inline 输入方式。整个过程跨多个工具反复切换，说明这块逻辑一开始没捋顺，需要来回试错。

### KeyPulse HUD 功能优化代码提交

14:21 你执行 git commit，提交信息是“fix(hud): signals 改吃日报「今日主线」主题 + 今日的事儿改回 inline 输入”，修改了 4 个文件。提交前还在 Terminal 里和 Claude 确认“要不要现在就第一件事 commit？”，看得出来对这次改动比较谨慎。

### KeyPulse HUD 运行行为确认

15:44 你验证了 HUD 的退出机制：退出后状态栏图标会消失，但后台采集 daemon 会继续运行。这应该是为了保证数据采集不中断，同时不占用界面空间。

### CorpusFlow 产品范围收缩决策

09:26 你明确了 CorpusFlow 的核心用户是测试团队、数据 PM 和算法工程师，短期最强需求是批量上传 100-200 seed、高质量 query 生成、参数泛化等。最终判断是**降级做**：先打穿批量 Query 生成，多轮上下文后续补轻量模板，精调、代码生成、完整评测平台都放后面。

### CorpusFlow 批量 Query 生成逻辑问题排查

11:48 你发现微调+多轮逻辑有问题，跟 Codex 指出“按照 llamafactory，system 是可选，但 instruction 是必填（就是提示词）、input 是当前 query、output 是最终输出，你把关系理清楚行不行？”，看来之前的逻辑混淆影响了功能生效。

### GitNexus 测试与 Docker 配置修复

今天 GitNexus 有两个提交：02:15 修复了 CI 超时问题（widen worker pool retry timeout），04:40 给容器添加了专用健康检查端点。都是比较基础的稳定性优化。

### 杂项

09:46 看了奇瑞股价波动原因分析，17:25 访问了 Apple 报告问题页面，19:54 浏览望京个人转租信息，22:15 查看 BOSS 直聘简历模板。这些都是零散的信息浏览，没持续深入。

## 今天的卡壳

KeyPulse HUD 信号源逻辑改了挺久，主要是没一次把日报主线 H3 的解析逻辑理顺，导致在 Terminal 和编辑器之间来回切换试错。直到下午提交代码前还在确认细节，说明这块比预想的要复杂。

## 明日的锚点

> 明天我想：______
>
> _写一句话留给明天的自己_