# Cluster Golden Set

These samples were captured from real `Topics` observations on `2026-04-20`.

- Add any new sample that motivates a new cluster strategy.
- Add a positive sample whenever user feedback shows a topic was rejected incorrectly.
- Keep this file lean and representative; do not add duplicates unless they cover a distinct regression.
